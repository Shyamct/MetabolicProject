app.controller('dashboardCtrl', function ($scope, ASSETS, dataFactory, $rootScope) {
    //$rootScope.SiteMenu = generateMenus();
    //log(generateMenus());
    //$rootScope.SiteMenu=UtilsCache.getSession('MENUASSIGN');
    //log($rootScope.SiteMenu);
});












